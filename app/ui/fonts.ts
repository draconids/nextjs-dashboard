import { Inter, Lusitana, Nanum_Gothic } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const nanumGothic = Nanum_Gothic({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});
